This is a test of using github

Steps in creating a new repo:

  git init
  git commit -m "first commit"
  git remote add origin https://github.com/vicwlee/test1.git
  git push -u origin master
  >> At this point, the system will ask for your username and password
  
